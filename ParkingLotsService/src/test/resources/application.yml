spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      auto-offset-reset: earliest
      group-id: baeldung
    producer:
      bootstrap-servers: localhost:9092
test:
  topic: parking